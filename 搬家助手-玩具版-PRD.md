# 搬家助手 · 玩具版 PRD

| 项目 | 内容 |
|---|---|
| 版本 | v1.0 (Toy Edition) |
| 日期 | 2026-07-12 |
| 现有产品 | https://banjia-two.vercel.app/ |
| 定位 | **个人作品 / 送朋友的小玩具，不是生意** |
| 预计工时 | **1–2 个晚上** |

---

## 0. 这份文档和上一份的区别

上一份 PRD（B2B2C / 生意版）已经作废。经过验证，商业模型不成立：

> 1 个中介 × 4.5 单/月 × ¥40/条线索 = **¥180/月**
> 需要 111 个免费中介才能月入 2 万，且无杠杆、无留存、无绑定 → **不是生意。**

**本文档的目标彻底变了：**

| | 生意版（作废） | 玩具版（本文档） |
|---|---|---|
| 目标 | 赚钱 | **自己爽 + 朋友觉得牛逼** |
| 用户 | 陌生人 | **你 + 你朋友** |
| 谁填信息 | 用户填 | **你填** |
| 后端 | 需要 | **不需要** |
| 引导流程 | 10 步 | **0 步** |
| 工时 | 6+ 周 | **1–2 晚** |

### 唯一的设计原则

> **你花的时间要少，朋友打开的那一眼要"哇"。**
>
> 任何不服务于这一条的功能，一律砍掉。

---

## 1. 核心机制：零后端

**把配置塞进 URL，不做数据库、不做登录、不做账号。**

```
你的操作：
  1. 打开 /config（只有你知道）
  2. 填 6 个字段
  3. 点「生成链接」→ 一键复制
  4. 微信发给朋友
       ↓
朋友的体验：
  点开链接 → 直接看到一份写着他名字的搬家计划
           → 「卧槽你还有这个？」  ← 目的达成
```

**链接形态：**

```
https://banjia-two.vercel.app/?d=eyJuYW1lIjoi5byg5LiJIiwi...
                                 ↑ base64 编码的 JSON
```

托管：Vercel 静态托管即可，**零成本**。

---

## 2. 数据结构

把现在硬编码的 `17 Dava → 25 New Dawn` 抽成一个对象：

```typescript
interface MoveConfig {
  name: string;        // 朋友的名字 ← 装 x 关键
  from: string;        // "上海 · 徐汇"
  to: string;          // "杭州 · 西湖"
  date: string;        // "2026-08-15"
  size: 'studio' | '1br' | '2br' | '3br';
  template: 'local' | 'long';   // 同城 | 跨城
}
```

**就这 6 个字段，不要再多了。** 每多一个字段，你就多一分「懒得填」的风险，而这个项目死于「懒得做完」的概率远高于死于「功能不够」。

### 编解码

```javascript
// 生成链接
const link = location.origin + "?d=" +
  btoa(encodeURIComponent(JSON.stringify(config)));

// 读取链接
const params = new URLSearchParams(location.search);
const raw = params.get("d");
const config = raw
  ? JSON.parse(decodeURIComponent(atob(raw)))
  : DEFAULT_CONFIG;   // 无参数时给个 demo 配置，别白屏
```

> ⚠️ **注意中文编码：** 必须 `encodeURIComponent` 后再 `btoa`，否则中文名字会炸。

---

## 3. 模板：硬编码，不做引擎

**不要写规则引擎。** 就写死两个数组：

```javascript
const TEMPLATES = {
  local: [   // 同城搬家
    { title: "断舍离：清点不要的东西", offset: -21, phase: "准备" },
    { title: "预约搬家公司 / 租车",   offset: -14, phase: "准备" },
    { title: "通知房东退租",          offset: -30, phase: "准备" },
    // ...
  ],
  long: [    // 跨城搬家
    // 在 local 基础上增加：长途物流、临时住宿、地址变更 等
  ]
};
```

- `offset` = 相对搬家日的天数（负数 = 搬家前）
- 任务日期 = `date + offset`，**一个函数就搞定，不需要排程器**

### 内容来源

**直接复用你现在的清单**，只做一件事：**通用化改写**——把「17 Dava 的钥匙」这类只对你成立的内容，改成任何人都适用的措辞。

> 户型（`size`）**只影响文案里的数字**（如"预计需要 40 个纸箱"），**不影响任务集合**。别为它做分支逻辑，不值得。

---

## 4. 页面

### 4.1 `/config` — 配置页（只有你看）

```
┌────────────────────────────┐
│  [名字]                     │
│  [起点]  →  [终点]          │
│  [搬家日期]                 │
│  [户型 ▾]  [同城/跨城 ▾]    │
│                            │
│     [ 生成链接 ]            │
│  ────────────────────      │
│  https://...?d=eyJ...      │
│     [ 一键复制 ]            │
└────────────────────────────┘
```

**丑没关系。只有你会看到它。** 不要在这里花时间。

### 4.2 `/` — 计划页（朋友看）

**这里是唯一值得花时间的地方。**

```
┌──────────────────────────────┐
│                              │
│    李雷的搬家计划              │  ← 名字，第一眼
│    上海 · 徐汇 → 杭州 · 西湖   │
│                              │
│    ⏳ 距离搬家还有 34 天       │  ← 倒计时，廉价但有效
│                              │
├──────────────────────────────┤
│  ▓▓▓▓▓░░░░░  12/47 已完成     │  ← 进度条
├──────────────────────────────┤
│  📦 准备阶段                  │
│  ☑ 断舍离：清点不要的东西      │
│  ☐ 通知房东退租     · 7月20日  │
│  ☐ 预约搬家公司     · 8月1日   │
│  ...                         │
├──────────────────────────────┤
│         Made by 你的名字       │  ← 落款，别忘了 😄
└──────────────────────────────┘
```

---

## 5. 装 x 清单（钱要花在这五个地方）

> 目的是让朋友「哇」，所以工时**优先**投在这里，其他一概能省则省。

| # | 要点 | 为什么 |
|---|---|---|
| 1 | **首屏写他的名字和地址** | 决定了他是「哇」还是「哦」。**最重要的一屏。** |
| 2 | **倒计时**「还有 34 天」 | 极廉价，极有效 |
| 3 | **勾选有爽感**（小动画 / 划掉线） | 这是他唯一会做的交互，做得舒服点 |
| 4 | **移动端优先** | 他 99% 从微信点开，用手机看。**桌面端可以完全不管。** |
| 5 | **底部落款** | 装 x 的核心，署名权 |

**进度保存：** `localStorage`，key 用 config 的 hash。**不需要后端。** 换手机就没了——无所谓，这是玩具。

---

## 6. 明确不做（Out of Scope）

以下全部砍掉，一个都不要做：

- ❌ **引导问卷** — **你来填，别让朋友填。** 朋友一看要填表就关了
- ❌ 账号 / 登录 / 注册
- ❌ 数据库 / 后端 / 云同步
- ❌ 规则引擎 / 任务模块库
- ❌ 成本估算 — **不准，反而暴露**
- ❌ 风险检测
- ❌ 埋点 / 漏斗分析
- ❌ SEO / Landing Page
- ❌ 桌面端适配

> 这些都是「生意」才需要的东西。**玩具不需要，而且做了会让你做不完。**

---

## 7. 交付步骤

| Step | 内容 | 预计 |
|---|---|---|
| 1 | 抽出 `MoveConfig`，替换硬编码的 `17 Dava → 25 New Dawn` | 30 min |
| 2 | 写 URL 编解码工具函数（注意中文） | 20 min |
| 3 | 把现有清单通用化改写，做成 `local` / `long` 两套 | **1–2 h**（内容活，最耗时） |
| 4 | `/config` 配置页（丑的就行） | 40 min |
| 5 | 首屏改造：名字 + 倒计时 + 进度条 | 1 h |
| 6 | 移动端样式过一遍 | 40 min |
| 7 | 发给第一个朋友 🎉 | — |

**总计：一到两个晚上。**

### 完成的定义

> **朋友在微信里点开链接，第一眼看到自己的名字，说了句「牛逼」。**
>
> 做到这个就完事了。不要再加功能。

---

## 附录：本次讨论的结论存档

**这个项目不是一门生意，原因（已验证，非推测）：**

1. **时序错位** — 用户需要搬家清单时，租/购房交易已完成，获客价值已被别人赚走
2. **零留存** — 搬完即走，1–2 年内不会再来
3. **无杠杆** — 收入随中介数量线性增长，运营成本同步线性增长，曲线永不分叉
4. **单价过低** — 搬家公司只愿为线索付 ¥30–50，中介月产能仅 4.5 单

**但这次验证本身是成功的：** 用两个下午、几通电话杀死一个坏主意，远好于花三个月写完代码才发现。

**下次选方向前，先问这四个问题（动手之前）：**

1. **谁付钱？**（不是「谁用」）
2. **他为什么必须付？**（省钱 / 赚钱 → 可能付；"体验好" → 通常没人付）
3. **他会付第二次吗？**（一次性生意极难做）
4. **我增长时，成本会不会同步增长？**（会 → 没有杠杆）

> 本次项目：四个问题里三个是「否」。
> 你现在能在动手之前看出来了 —— **这是这个项目真正的产出。**
